#include<stdio.h>

void main(){
    /*
    // ARITHMETIC OPERATORS
    printf("ARITHMETIC OPERATIONS \n");
    int var1=7;
    int var2=5;

    printf("Addition of Var1 & Var2: %i \n", var1 + var2); // Addition
    printf("Subtraction of Var1 & Var2: %i \n", var1 - var2); // subtraction
    printf("Multiplication of Var1 & Var2: %i \n", var1 * var2); // multiplication

    float a=9.6;
    float b=4.6;
    printf("Division of a & b: %f \n", a / b); // division
    printf("Remainder of Var1 Var2: %i \n", var1 % var2); // Modulus

    // RELATIONAL OPERATORS
    printf("\n--Relational Operations--\n");
    if(a!=b) {
        printf("VALUE OF A & B ARE'NT EQUAL");
    }

    

   // LOGICAL OPERATORS
   printf("\n -- LOGICAL OPERATIONS -- \n");
   int firstVar = -4;
   int secondVar = 5;
   int result;
   result = (firstVar>=0) && (secondVar>=0); 
   // f && t = false =0
   printf("Both Values are Positive: %i", result);

   result = (firstVar>=0) || (secondVar>=0); 
   // false || true = true = 1
   printf("\nAt least one value is Positive: %i", result);

    result = !(firstVar>=0);
    // result = !(false) = !(0)= true = 1
   printf("\nfirst Value is negative: %i", result);
    

   // INCREMENT / DECREMENT OPERATORS
   int i=12;
   int resInc;
   resInc = ++i; // increment
   printf("Value after Increment: %i \n", resInc);

   int j=6;
   int resDec;
   resDec = --j; // Decrement
   printf("Value after Decrement: %i \n", resDec);
   

  int a = 3;  // simple assignment
  printf("%i \n", a);
  // Compund Assignment
  a+=2;   // a= a+2;
  printf("After Using += operator %i \n", a);
  a-=2;   // a= a-2;
  printf("After Using -= operator %i \n", a);
  a*=2;   
  printf("After Using *= operator %i \n", a);
  a/=2;   // a= a/2;
  printf("After Using /= operator %i \n", a);
  a%=2;   // a= a%2;
  printf("After Using %%= operator %i \n", a);
  
  // BIT-WISE SHIFT / OPERATIONS
  a<<=2;  // 100 = 4
  printf("After Using <<= operator %i \n", a);
  a>>=1;  
  printf("After Using >>= operator %i \n", a);
  a&=2; // 10 & 10
  printf("After Using &= operator %i \n", a); 

  a^=1;  // XOR
  printf("After Using ^= operator %i \n", a);
  a|=1;  // inclusive OR
  printf("After Using |= operator %i \n", a);
  */
 float anExpression = 10/(2+4);
 printf("Result = %f", anExpression);



  }